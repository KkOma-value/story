# Production environment - switch to real API
VITE_API_MODE=real

# Backend API base URL for production.
# Leave empty if using a reverse proxy (e.g., Nginx) to forward /api/* to backend.
# Otherwise set to the full backend origin, e.g.: https://api.example.com
VITE_API_BASE_URL=
